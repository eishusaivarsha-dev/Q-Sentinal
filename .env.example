# Copy to .env (never commit .env)
QSENTINEL_PROFILE=fast              # fast | full
CORS_ORIGINS=http://localhost:5173
VITE_API_URL=http://localhost:8000
# QSENTINEL_REDIS_URL=redis://localhost:6379/0   # optional telemetry mirror (pip install -e .[redis])
# Phase 4:
# QISKIT_IBM_TOKEN=
# ANTHROPIC_API_KEY=                # ops plane narration only
